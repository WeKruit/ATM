# Staging destination for Kamal deploys
# TEMPORARY — IPs will go stale on next ASG stop/start
servers:
  web:
    hosts:
      - 34.200.225.8        # gh-worker-1 (ASG i-0de0d236d543467f0)
  workers:
    hosts:
      - 34.200.225.8        # gh-worker-1 (ASG i-0de0d236d543467f0)

env:
  clear:
    GH_ENVIRONMENT: staging
